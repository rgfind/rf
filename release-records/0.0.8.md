# rf 0.0.8 contract classification

Status: additive, pending final release verification.

- External-tool health diagnostics add typed fields to `doctor` and `find`.
- `--matches` and `--max-matches` add opt-in evidence fields to `content`,
  `find`, and `why`. Default rows and default cursor identity do not change.
- Contract governance adds a policy URL, schema versions, fixed-limit metadata,
  and warning detail metadata.

No existing valid request changes its default result, exit-code meaning, output
mode, or cursor identity. Release must stop if the final fixture or policy
metadata does not support this classification.
